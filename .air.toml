root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/app.exe ."
bin = "./tmp/app.exe"

include_ext = ["go", "tpl", "tmpl", "html"]

exclude_dir = ["tmp", "vendor", "storage"]

delay = 1000

stop_on_error = true

send_interrupt = true

kill_delay = 500